#include <stdio.h>

int main() {
    int n, sum = 0, i;

    printf("Enter a positive number N: ");
    scanf("%d", &n);

    for (i = 1; i <= n; i++) {
        sum += i;  // add i to sum
    }

    printf("Sum of first %d natural numbers = %d\n", n, sum);

    return 0;
}
